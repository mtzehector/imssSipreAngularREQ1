import { CalendarioDirective } from './calendario.directive';

describe('CalendarioDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarioDirective();
    expect(directive).toBeTruthy();
  });
});
